import styled from "styled-components";

export const ItemParamsStyled = styled.div`
  display: flex;
  flex-direction: row;
  flex-wrap: nowrap;
  justify-content: space-around;
  align-items: center;
  align-content: center;
  padding: 5px;
  & > p {
  margin-bottom: 0;
  }
`